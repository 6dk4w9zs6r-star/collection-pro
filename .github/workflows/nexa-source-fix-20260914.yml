name: NEXA-MF direct source fix

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/nexa-source-fix-20260914.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply Late active-state correction
        run: python3 scripts/fix_late_active_logic_20260914.py
      - name: Validate and commit
        run: |
          git config user.name "nexa-mf-fix-bot"
          git config user.email "nexa-mf-fix-bot@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Fix Late eligibility after partial payments"
          git push origin HEAD:main
